# Changelog — Version 1.2.2 (pending — submit July 7)

## User-Facing Changes
1. Face ID grace period: 30-second window when switching apps — Record screen stays open
2. Add Account: font reduced to .body, fields combined into single section, currency symbol inside border
3. Edit Balance: amount field to .body font, currency symbol inside border
4. Rename: .body font added explicitly, padding adjusted (7pt) for visual consistency
5. Record: amounts to .body font, hint text bumped to 14pt
6. All form fields unified: .body font, rounded border, currency symbol inside border where applicable

## Developer-Only
3. FundraApp: backgroundedAt timestamp replaces immediate lock on scenePhase change
